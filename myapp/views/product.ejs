<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
        <% for( let i = 0; i < listaProducto.length; i++ ) { %>
            <li><%=listaProducto[i].imagenArchivo%></li>
            <li><%=listaProducto[i].descrpcion%></li>
        <% } %>
        <% for( let i = 0; i < productoComentarios.length; i++ ) { %>
            <li><%=productoComentarios[i].comentario%></li>
        <% } %>
    </ul>
</body>
</html>