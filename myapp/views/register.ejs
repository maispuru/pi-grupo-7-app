<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
       <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/bootstrap-grid.min.css">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>DigitalMac</title>
</head>
<body>
    <%- include('./partials/header.ejs') %>
    <div class="container products-wrapper">
    <div class="row">
    <div class="col-12">
      <h2 class="products-title">Registrate</h2>
    </div>

    <div class="col-12 col-md-10 col-lg-8 mx-auto">
      <form action="/users/register" method="POST" class="register-form">

        <label for="email">Ingrese su correo electrónico:</label><br>
        <input type="email" name="email" value=""><br><br>
        <% if (error.email) { %><p class="error"><%= error.email %></p><% } %>

        <label for="usuario">Usuario:</label><br>
        <input type="text" name="usuario" value=""><br><br>
        <% if (error.usuario) { %><p class="error"><%= error.usuario %></p><% } %>

        <label for="contrasena">Contrasena:</label><br>
        <input type="password" name="contrasena" value=""><br><br>
        <% if (error.contrasena) { %><p class="error"><%= error.contrasena %></p><% } %>

        <label for="fotoPerfil">Foto de perfil:</label><br>
        <input type="file" name="fotoPerfil"><br><br>
        <% if (error.fotoPerfil) { %><p class="error"><%= error.fotoPerfil %></p><% } %>

        <button type="submit">Registrarse</button>
      </form>    

    </div>
  </div>
</div>


<%- include('./partials/footer.ejs') %>
</body>

</html>